import React from "react";
import { Header, Body, Footer } from "./components";

const Home = () => {
    return (
        <div>
            <Header></Header>
            <Body></Body>
            <Footer></Footer>
        </div>
    );
};

export default Home;
